import React from 'react';
import {Typography} from 'antd';

const {Title} = Typography;

const PanelPage = () => {
    return <div>
        <Title level={2}>Steps to help prevent the spread of COVID-19 if you are sick</Title>
    </div>;
};

export default PanelPage;
